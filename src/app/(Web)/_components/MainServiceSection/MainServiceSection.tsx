import { MainReview } from './_components';

const MainServiceSection = () => {
  return (
    <section
      id='calc_header_height'
      className='w-full py-[4rem] flex flex-col items-center justify-center px-[1.6rem]'
    >
      <MainReview />
    </section>
  );
};

export default MainServiceSection;
